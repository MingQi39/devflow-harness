# 主机 Nginx：flow.houmq.cn → Docker frontend (127.0.0.1:8082)

# HTTP（当前访问：http://flow.houmq.cn/）
server {
    listen 80;
    server_name flow.houmq.cn;

    location / {
        proxy_pass http://127.0.0.1:8082;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_buffering off;
        proxy_read_timeout 3600s;
    }
}

# HTTPS（可选：申请证书后取消注释并 reload nginx）
# server {
#     listen 443 ssl http2;
#     server_name flow.houmq.cn;
#
#     ssl_certificate     /etc/letsencrypt/live/flow.houmq.cn/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/flow.houmq.cn/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:8082;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_buffering off;
#         proxy_read_timeout 3600s;
#     }
# }
